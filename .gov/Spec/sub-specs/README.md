# StratAtlas Sub-Specs

Date: 2026-03-04

This folder holds iteration-level sub-specs required before implementation merges.

- Use `.gov/templates/SUB_SPEC_TEMPLATE.md` when authoring new sub-specs.
- Keep one primary sub-spec per iteration (I0..I10), then add supplements only when needed.
- Sub-spec approval is required before implementation for that iteration begins.

